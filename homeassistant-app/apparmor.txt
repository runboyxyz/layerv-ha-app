#include <tunables/global>

profile layerv_ha_gateway flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Keep the App behind Home Assistant's protected container boundary while
  # permitting Python and the bundled connector to use their packaged runtime
  # files and private container log paths. This compatibility profile can be
  # narrowed from real Home Assistant audit denials in a later release.
  file,
  network,
  capability,
  signal (send, receive),
}

#include <tunables/global>

profile layerv_ha_gateway flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  signal (send, receive),

  /usr/local/bin/python ix,
  /usr/local/bin/python3 ix,
  /usr/local/bin/python3.12 ix,
  /usr/local/bin/qurl-connector ix,
  /usr/local/bin/*.py r,

  /app/** r,
  /usr/local/lib/** mr,
  /usr/lib/** mr,
  /lib/** mr,
  /lib64/** mr,
  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,

  /dev/null rw,
  /dev/random r,
  /dev/urandom r,

  /data/** rwk,
  /tmp/** rwk,
}

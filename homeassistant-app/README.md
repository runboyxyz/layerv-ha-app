# LayerV Home Assistant Gateway

Create reusable Home Assistant access pages and issue independently revocable,
expiring LayerV qURLs to named users.

The App communicates with Home Assistant through Supervisor, runs the gateway
and qURL Connector together, and keeps credentials, page data, and connector
identity in persistent App storage.

